{
    "pre-commit": {
        "enabled": true,
        "actions": [
            {
                "action": "vendor/bin/ecs check --fix -q -n {$STAGED_FILES|replace:backend/|with:|of-type:php} && git add {$STAGED_FILES|of-type:php}"
            },
            {
                "action": "\\CaptainHook\\App\\Hook\\Composer\\Action\\CheckLockFile",
                "options": []
            }
        ]
    }
}
